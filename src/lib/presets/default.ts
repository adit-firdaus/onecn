import type { OneCNConfig } from "../config";

/**
 * Default preset — matches standard shadcn/ui baseline.
 * This is effectively a no-op preset; it produces no overrides.
 */
export const defaultPreset: OneCNConfig = {};
